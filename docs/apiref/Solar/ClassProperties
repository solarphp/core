+ Class Properties

++ Public

* Solar::$config -- The values read in from the configuration file.

* Solar::$dir -- Where the Solar arch-class is in the filesystem.

* Solar::$locale -- Locale strings for all classes.

* Solar::$registry -- Object registry.

++ Protected

* Solar::$_locale_code -- The current locale code being used by Solar.

* Solar::$_status -- Status flag (whether Solar has started or not).

++ Private

None.