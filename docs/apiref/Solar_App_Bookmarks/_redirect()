//Inherited from Solar_Controller_Page::_redirect().//

<function>

access:   protected

name: _redirect

param: Solar_Uri|string, $spec

param: bool, $external, false

return: void

</function>

Redirects to another page and action.

* Parameters:

 * Solar_Uri|string //$spec//: The URI to redirect to.

 * bool //$external//: Treat as a link to an external URI; i.e., not as a page-action link.

* Returns void

Redirects to another page and action.