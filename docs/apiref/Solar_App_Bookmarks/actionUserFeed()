<function>

access:   public

name: actionUserFeed

param: string, $owner_handle, NULL

param: string|array, $tags, NULL

return: void

</function>

Shows all bookmarks for an owner as an RSS feed, optionally 
filtered by tag.

* Parameters:

 * string //$owner_handle//: The owner to show bookmarks for.

 * string|array //$tags//: An optional set of tags to filter by; either a space- (or plus-) separated list of tags, or a sequential array of tags.

* Returns void