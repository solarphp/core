//Inherited from Solar_App::_setup().//

<function>

access:   public

name: _setup

return: void

</function>

Sets up the Solar_App environment.

* Parameters: none.

* Returns void

Sets up the Solar_App environment.

Registers 'sql', 'user', and 'content' objects, and sets the
layout title to the class name.