<function>

access:   public

name: fetchByOwnerUri

param: string, $handle

param: string, $uri

return: array

</function>

Fetches a bookmark by owner_handle (username) and URI.

* Parameters:

 * string //$handle//: The owner_handle (username).

 * string //$uri//: The URI to look form

* Returns array: The node data.

Useful for seeing if an owner has already bookmarked a URI.