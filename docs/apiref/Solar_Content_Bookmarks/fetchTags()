<function>

access:   public

name: fetchTags

param: string, $handle, NULL

return: array

</function>

Fetches a list of all bookmark tags.

* Parameters:

 * string //$handle//: The owner_handle (username) to list bookmark tags for; if empty, lists for all owners.

* Returns array: An array where the key is the tag name and the value is the number of times that tag appears.

You can specify an owner_handle (username) to limit the list.