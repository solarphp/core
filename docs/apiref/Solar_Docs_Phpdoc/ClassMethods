+ Class Methods

++ Public

* Solar_Docs_Phpdoc::parse() -- Returns docblock comment parsed into summary, narrative, and
technical information portions.

* Solar_Docs_Phpdoc::parseCategory() -- Parses an @category line into $this->_info.

* Solar_Docs_Phpdoc::parseException() -- Parses an @exception line into $this->_info; alias for @throws.

* Solar_Docs_Phpdoc::parsePackage() -- Parses an @package line into $this->_info.

* Solar_Docs_Phpdoc::parseParam() -- Parses an @param line into $this->_info.

* Solar_Docs_Phpdoc::parseReturn() -- Parses an @return line into $this->_info.

* Solar_Docs_Phpdoc::parseSee() -- Parses an @see line into $this->_info.

* Solar_Docs_Phpdoc::parseSubpackage() -- Parses an @subpackage line into $this->_info.

* Solar_Docs_Phpdoc::parseThrows() -- Parses an @throws line into $this->_info.

* Solar_Docs_Phpdoc::parseTodo() -- Parses an @todo line into $this->_info.

* Solar_Docs_Phpdoc::parseVar() -- Parses an @var line into $this->_info.

Inherited Methods:

* Solar_Base::__construct() -- Constructor.

* Solar_Base::apiVersion() -- Reports the API version for this class.

* Solar_Base::locale() -- Looks up locale strings based on a key.

++ Protected

* Solar_Docs_Phpdoc::_1part() -- Parses a one-part block line.

* Solar_Docs_Phpdoc::_2part() -- Parses a two-part block line.

* Solar_Docs_Phpdoc::_loadInfo() -- Gets the technical information from a docblock comment.

Inherited Methods:

* Solar_Base::_exception() -- Convenience method for returning exceptions with localized text.

++ Private

None.