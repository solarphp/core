<function>

access:   protected

name: _2part

param: string, $line

return: array

</function>

Parses a two-part block line.

* Parameters:

 * string //$line//: The block line.

* Returns array: An array with keys 'type' (the first part) and 'summ' (the second part).