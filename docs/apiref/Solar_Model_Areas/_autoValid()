//Inherited from Solar_Sql_Table::_autoValid().//

<function>

access:   protected

name: _autoValid

param: array, &$data

return: void

</function>

Validates and recasts an array of input/update data in-place.

* Parameters:

 * array //$data//: An associative array of data as (field => value). Note that this is a reference; the array will be modified in-place.

* Returns void