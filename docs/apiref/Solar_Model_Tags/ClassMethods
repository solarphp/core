+ Class Methods

++ Public

* Solar_Model_Tags::__construct() -- Constructor.

* Solar_Model_Tags::__get() -- Allows reading of protected properties.

* Solar_Model_Tags::apiVersion() -- Reports the API version for this class.

* Solar_Model_Tags::asArray() -- Normalizes tag arrays.

* Solar_Model_Tags::asString() -- Normalizes tag strings.

* Solar_Model_Tags::delete() -- Deletes rows in the table based on a WHERE clause.

* Solar_Model_Tags::fetchAllByNodeId() -- Fetch all tags on one node_id.

* Solar_Model_Tags::fetchDefault() -- Returns a data array with column keys and default values.

* Solar_Model_Tags::increment() -- Increments and returns the sequence value for a column.

* Solar_Model_Tags::insert() -- Validates and inserts data into the table.

* Solar_Model_Tags::locale() -- Looks up locale strings based on a key.

* Solar_Model_Tags::paging() -- Sets the number of rows per page.

* Solar_Model_Tags::refresh() -- "Refreshes" the tags for a node_id by diff.

* Solar_Model_Tags::save() -- Inserts or updates a single row based on its ID.

* Solar_Model_Tags::select() -- Convenience method to select rows from this table.

* Solar_Model_Tags::update() -- Validates and updates data in the table based on a WHERE clause.

++ Protected

* Solar_Model_Tags::_autoCreate() -- Creates the table in the database if it does not already exist.

* Solar_Model_Tags::_autoSetup() -- Fixes the $col and $idx properties after user setup.

* Solar_Model_Tags::_autoValid() -- Validates and recasts an array of input/update data in-place.

* Solar_Model_Tags::_diff() -- Determines the diff (delete/insert matrix) between two tag sets.

* Solar_Model_Tags::_exception() -- Convenience method for returning exceptions with localized text.

* Solar_Model_Tags::_setup() -- Schema setup.

++ Private

None.