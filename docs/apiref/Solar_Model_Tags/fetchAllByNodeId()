<function>

access:   public

name: fetchAllByNodeId

param: int, $node_id

return: array

</function>

Fetch all tags on one node_id.

* Parameters:

 * int //$node_id//: The node_id to fetch tags for.

* Returns array: An array of tags on that node.

Fetch all tags on one node_id.