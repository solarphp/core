+ Class Methods

++ Public

* Solar_Sql_Driver_Mssql::__construct() -- Constructor.

* Solar_Sql_Driver_Mssql::apiVersion() -- Reports the API version for this class.

* Solar_Sql_Driver_Mssql::begin() -- Leave autocommit mode and begin a transaction.

* Solar_Sql_Driver_Mssql::buildCreateTable() -- Builds a CREATE TABLE command string.

* Solar_Sql_Driver_Mssql::buildSelect() -- Adds a LIMIT clause (or equivalent) to a SELECT statement.

* Solar_Sql_Driver_Mssql::commit() -- Commit a transaction and return to autocommit mode.

* Solar_Sql_Driver_Mssql::createSequence() -- Creates a sequence, optionally starting at a certain number.

* Solar_Sql_Driver_Mssql::dropSequence() -- Drops a sequence.

* Solar_Sql_Driver_Mssql::exec() -- Prepares and executes an SQL statement with bound data.

* Solar_Sql_Driver_Mssql::listTables() -- Returns a list of database tables.

* Solar_Sql_Driver_Mssql::locale() -- Looks up locale strings based on a key.

* Solar_Sql_Driver_Mssql::nativeColTypes() -- Returns a list of native column types.

* Solar_Sql_Driver_Mssql::nextSequence() -- Gets a sequence number; creates the sequence if it does not exist.

* Solar_Sql_Driver_Mssql::quote() -- Safely quotes a value for an SQL statement.

* Solar_Sql_Driver_Mssql::rollback() -- Roll back a transaction and return to autocommit mode.

++ Protected

* Solar_Sql_Driver_Mssql::_connect() -- Creates a PDO object and connects to the database.

* Solar_Sql_Driver_Mssql::_dsn() -- Creates a PDO-style DSN.

* Solar_Sql_Driver_Mssql::_exception() -- Convenience method for returning exceptions with localized text.

++ Private

None.