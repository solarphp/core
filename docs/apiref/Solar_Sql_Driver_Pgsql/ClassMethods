+ Class Methods

++ Public

* Solar_Sql_Driver_Pgsql::__construct() -- Constructor.

* Solar_Sql_Driver_Pgsql::apiVersion() -- Reports the API version for this class.

* Solar_Sql_Driver_Pgsql::begin() -- Leave autocommit mode and begin a transaction.

* Solar_Sql_Driver_Pgsql::buildCreateTable() -- Builds a CREATE TABLE statment.

* Solar_Sql_Driver_Pgsql::buildSelect() -- Builds a SELECT statement from its component parts.

* Solar_Sql_Driver_Pgsql::commit() -- Commit a transaction and return to autocommit mode.

* Solar_Sql_Driver_Pgsql::createSequence() -- Creates a sequence, optionally starting at a certain number.

* Solar_Sql_Driver_Pgsql::dropSequence() -- Drops a sequence.

* Solar_Sql_Driver_Pgsql::exec() -- Prepares and executes an SQL statement with bound data.

* Solar_Sql_Driver_Pgsql::listTables() -- Returns the SQL statement to get a list of database tables.

* Solar_Sql_Driver_Pgsql::locale() -- Looks up locale strings based on a key.

* Solar_Sql_Driver_Pgsql::nativeColTypes() -- Returns a list of native column types.

* Solar_Sql_Driver_Pgsql::nextSequence() -- Gets a sequence number; creates the sequence if it does not exist.

* Solar_Sql_Driver_Pgsql::quote() -- Safely quotes a value for an SQL statement.

* Solar_Sql_Driver_Pgsql::rollback() -- Roll back a transaction and return to autocommit mode.

++ Protected

* Solar_Sql_Driver_Pgsql::_connect() -- Creates a PDO object and connects to the database.

* Solar_Sql_Driver_Pgsql::_dsn() -- Creates a PDO-style DSN.

* Solar_Sql_Driver_Pgsql::_exception() -- Convenience method for returning exceptions with localized text.

++ Private

None.