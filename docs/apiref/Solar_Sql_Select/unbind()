<function>

access:   public

name: unbind

param: mixed, $spec, NULL

return: void

</function>

Unsets bound data.

* Parameters:

 * mixed //$spec//: The key to unset.  If a string, unsets that one bound value; if an array, unsets the list of values; if empty, unsets all bound values (the default).

* Returns void

Unsets bound data.