<function>

access:   public

name: increment

param: string, $name

return: int

</function>

Increments and returns the sequence value for a column.

* Parameters:

 * string //$name//: The column name.

* Returns int: The next sequence number for the column.

Increments and returns the sequence value for a column.