<function>

access:   public

name: findTests

param: Object id #35, $iter, NULL

return: void

</function>

Recursively iterates through a directory looking for test classes.

* Parameters:

 * Object id #35 //$iter//: Directory iterator.

* Returns void

Recursively iterates through a directory looking for test classes.

Skips dot-files and files that start with lower-case letters.