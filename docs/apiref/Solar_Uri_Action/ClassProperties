+ Class Properties

++ Public

Inherited Properties:

* Solar_Uri::$fragment() -- The fragment portion (e.

* Solar_Uri::$host() -- The host specification (e.

* Solar_Uri::$pass() -- The password, if any.

* Solar_Uri::$path() -- The path portion (e.

* Solar_Uri::$port() -- The port number (e.

* Solar_Uri::$query() -- Query string elements split apart into an array.

* Solar_Uri::$scheme() -- The scheme (e.

* Solar_Uri::$user() -- The username, if any.

++ Protected

* Solar_Uri_Action::$_Solar_Uri_Action -- User-provided configuration values.

Inherited Properties:

* Solar_Uri::$_Solar_Uri() -- User-provided configuration values.

* Solar_Base::$_config() -- Collection point for configuration values.

* Solar_Uri::$_encode_path() -- Url-encode only these characters in path elements.

++ Private

None.