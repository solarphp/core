//Inherited from Solar_Uri::_query2str().//

<function>

access:   protected

name: _query2str

param: array, $spec

return: string

</function>

Converts an array of query elements into a string.

* Parameters:

 * array //$spec//: The key-value pairs to convert into a query string.

* Returns string: A URI query string.

Converts an array of query elements into a string.

Modified from code written by nospam@fiderallalla.de, found at
http://php.net/parse_str.  Automatically urlencodes values.