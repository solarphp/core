<function>

access:   public

name: valid

param: string, $handle

param: string, $passwd

return: bool

</function>

Validates a username handle and password.

* Parameters:

 * string //$handle//: The username handle.

 * string //$passwd//: The plain-text password.

* Returns bool: True if valid, false if not.