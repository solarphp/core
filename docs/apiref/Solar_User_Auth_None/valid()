<function>

access:   public

name: valid

param: string, $handle

param: string, $passwd

return: bool

</function>

Validate a username and password.  Always fails.

* Parameters:

 * string //$handle//: Username to authenticate.

 * string //$passwd//: The plain-text password to use.

* Returns bool: True on success, false on failure.

Validate a username and password.  Always fails.