<function>

access:   public

name: __call

param: string, $method

param: array, $params

return: bool

</function>

Calls custom validation methods.

* Parameters:

 * string //$method//: The validation method to call.

 * array //$params//: The parameters for the validation method.

* Returns bool: True if valid, false if not.

Calls custom validation methods.