<function>

access:   public

name: __call

param: string, $name

param: array, $args

return: string

</function>

Executes a helper method with arbitrary parameters.

* Parameters:

 * string //$name//: The helper name.

 * array //$args//: The parameters passed to the helper.

* Returns string: The helper output.